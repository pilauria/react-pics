import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(<App />, document.querySelector('#root'));

// ****  App CHallenges:
/* - Get a search term from the const [state, dispatch] = useReducer(reducer, initialState, init) 
  - Use that search term to make a request to an outside API and fetch data
  -  Take the fetched images and show them on the screen in a list

*/
